

import { PokemonsContext } from "./GlobalStateContext";

const GlobalState = (props) => {

    return (

        <PokemonsContext.Provider>
            
        </PokemonsContext.Provider>
    )
}

export default GlobalState;